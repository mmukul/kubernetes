apiVersion: v1
kind: Service
metadata:
   name: myser
   labels: 
     app: nginx-lb
spec:
   selector:
     app: nginx-lb
   type: LoadBalancer
   ports:
    - nodePort: 32000
      port: 80
      targetPort: 80 
